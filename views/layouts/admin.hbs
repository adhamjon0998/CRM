<!DOCTYPE html>
<html lang="en">
{{> admin-head}}
<body class="hold-transition sidebar-mini layout-fixed">
<div class="wrapper">
 {{> admin-preloader}}
  {{> admin-navbar}}
    {{> admin-aside}}
  <div class="content-wrapper asd123">
    <div class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
          </div>
          <div class="col-sm-6">
          </div>
        </div>
      </div>
    </div>
    <section class="content projectContent">
        {{{ body }}}
    </section>
  </div>
  {{> admin-footer}}
</body>
</html>
